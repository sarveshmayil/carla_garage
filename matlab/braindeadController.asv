function u = braindeadController(setpoint, tspan)
%BRAINDEADCONTROLLER Summary of this function goes here
%   Detailed explanation goes here
x = setpoint(1);
y = setpoint(2);
psi = setpoint(3);
% viable inputs are steer angle and longitudinal force

end

